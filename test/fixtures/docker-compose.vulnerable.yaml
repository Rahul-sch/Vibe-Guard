# VG-DOCK-002 and VG-DOCK-003
version: '3.8'
services:
  app:
    image: myapp:latest
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "3000:3000"
